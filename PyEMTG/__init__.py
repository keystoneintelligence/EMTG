"""Python utilities shipped with EMTG.

The package marker is intentionally small.  Historical scripts that put the
``PyEMTG`` directory on ``sys.path`` and import modules directly remain
supported by the generated modules' import fallbacks.
"""
