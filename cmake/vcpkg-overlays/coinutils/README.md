# CoinUtils overlay

Copied from the pinned vcpkg revision in `cmake/vcpkg-revision.txt`.
The only build change supplies the static Fortran runtime closure for the
MinGW LAPACK probe, matching the existing IPOPT and MUMPS overlays.
Upstream source revision, patches, and EPL-2.0 license are unchanged.
