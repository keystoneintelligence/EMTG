# Expected no truth baseline

This integration scenario currently has an `.emtgopt` input without a checked-in `.emtg` truth file.
It is intentionally kept out of the fast unit folder set until a truth baseline is generated and reviewed.
